import "server-only"
export default function ServeroutePage(){
    console.log(`use multiple libraries,
        use envronment variables,
        interactive with a database,
        process confidential information.`)
        
    return "server result "
}